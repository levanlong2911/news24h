---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Navbar from '../components/Navbar.astro';
import '../styles/global.css';

const {
  pageTitle = 'News hot 24h',
  pageDescription = 'Hot news, updated quickly and accurately every day.',
  pageImage = 'https://admin.lifennew.com/default.jpg',
  pageUrl = 'https://admin.lifennew.com',
  isArticle = false,
  publishedAt = '',
  updatedAt = '',
  authorName = 'News hot 24h',
} = Astro.props;

let adData: any[] = [];
try {
  const res = await fetch('https://admin.lifennew.com/api/ads');
  if (res.ok) {
    const json = await res.json();
    adData = json.data;
  }
} catch (err) {
  console.error('Ad API fetch error:', err);
}

// 🔹 Chỉ xử lý `top` để tách script vào <head>
function extractScripts(adScript: string) {
  const scriptTags = [...adScript.matchAll(/<script[^>]*src=["']([^"']+)["'][^>]*><\/script>/gi)];
  const headScripts = scriptTags.map(m => m[0]);
  const bodyHtml = adScript.replace(/<script[^>]*src=["'][^"']+["'][^>]*><\/script>/gi, '');
  return { headScripts, bodyHtml };
}

const topAds = adData
  .filter(ad => ad.position === 'top')
  .map(ad => extractScripts(ad.script));

const middleAds = adData
  .filter(ad => ad.position === 'middle')
  .map(ad => ad.script); // nguyên gốc, không tách

const bottomAds = adData
  .filter(ad => ad.position === 'bottom')
  .map(ad => ad.script); // nguyên gốc, không tách
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>

    <!-- SEO -->
    <meta name="description" content={pageDescription} />
    <link rel="canonical" href={pageUrl} />

    <!-- Open Graph -->
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={pageDescription} />
    <meta property="og:image" content={pageImage} />
    <meta property="og:url" content={pageUrl} />
    <meta property="og:type" content={isArticle ? 'article' : 'website'} />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={pageDescription} />
    <meta name="twitter:image" content={pageImage} />

    <!-- Structured Data -->
    {isArticle && (
      <script type="application/ld+json">
        {JSON.stringify({
          "@context": "https://schema.org",
          "@type": "NewsArticle",
          headline: pageTitle,
          image: [pageImage],
          datePublished: publishedAt,
          dateModified: updatedAt || publishedAt,
          author: [{ "@type": "Person", name: authorName }],
        })}
      </script>
    )}

    <!-- Chèn <script> quảng cáo top vào <head> -->
    {topAds.map(({ headScripts }) =>
      headScripts.map(script => <Fragment set:html={script} />)
    )}
  </head>
  <body>
    <Navbar />

    <!-- Quảng cáo Top: phần DOM hiển thị -->
    {topAds.map(({ bodyHtml }) => (
      <div class="ad-top" set:html={bodyHtml} />
    ))}

    <main>
      <slot />

      <!-- Quảng cáo giữa bài (middle): nguyên đoạn -->
      {middleAds.map((html, i) => (
        <div class="ad-middle my-4" set:html={html} key={i} />
      ))}
    </main>

    <!-- Quảng cáo cuối trang (bottom): nguyên đoạn -->
    {bottomAds.map((html, i) => (
      <div class="ad-bottom mt-4" set:html={html} key={i} />
      ))}

    <Footer />
    <script type="module" src="/scripts/menu.js"></script>
  </body>
</html>
