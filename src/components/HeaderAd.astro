---
// import { extractScripts } from '../utils/ads.utils';
const { ads = {} } = Astro.props;
const items = ads.header || [];
---

{items.map((ad) => (
  <div
    class="ad-slot"
    data-ad-script={encodeURIComponent(ad.script)}
  />
))}
